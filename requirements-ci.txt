# Lightweight requirements for CI (lint + unit tests only).
# Does NOT include apache-airflow or pyspark — unit tests don't exercise those.
# Full requirements.txt is for local dev and Docker.

# ── Data ─────────────────────────────────────────────────────────────────────
pandas==2.1.4
pyarrow==14.0.2
numpy==1.26.3

# ── AWS ──────────────────────────────────────────────────────────────────────
boto3==1.34.14
s3fs==2023.12.2

# ── Kafka ────────────────────────────────────────────────────────────────────
confluent-kafka==2.3.0

# ── Database ─────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
redshift-connector==2.0.918

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.0
faker==22.2.0
loguru==0.7.2
tenacity==8.2.3

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==7.4.4
pytest-cov==4.1.0
moto[s3,redshift]==4.2.14
